# Get all users path

summary: Get all users
description: Get all users with pagination (20 items per page) in meta field.
tags:
  - Users
responses:
  '200':
    $ref: '../responses/get-all-users.response.yaml'
